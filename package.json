{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "A lightweight expense tracker with categories, filters, and local persistence.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "node --test"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3"
  }
}
